/// @description Insert description here
// You can write your code in this editor
life--;
//speed = bullet_speed 
depth = -y

if  (life <= 0) instance_destroy();

image_angle = direction;

//if collision_line(x, y, x + lengthdir_x(speed, direction), y + lengthdir_y(speed, direction), obj_colisor, false, true){

//	  audio_play_sound(snd_hit, 1, 0, 1, , random_range(0.8, 1.2));
//	instance_destroy()
//	instance_create_layer(x, y, "Bloom", obj_pop, { width: irandom_range(4, 16), lerpval : .8 });
//spark(x, y, 10, direction - 180, 40, 4, .9, #ffffff, #FFEB57, 4, 0);

//}

